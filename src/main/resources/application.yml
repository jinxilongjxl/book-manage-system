server:
  port: 7777
  servlet:
    context-path: /

# jdbc
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_book_system?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: jxl63138495
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
# jpa default
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
# thymeleaf config
  thymeleaf:
    cache: false
    suffix: .html
